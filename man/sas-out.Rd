% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sas-out.R
\name{sas-out}
\alias{sas-out}
\alias{as_outbase}
\alias{as_outcomp}
\alias{as_outdif}
\alias{as_outnoequal}
\title{SAS PROC COMPARE-style output datasets}
\usage{
as_outbase(x)

as_outcomp(x)

as_outdif(x)

as_outnoequal(x)
}
\arguments{
\item{x}{A \code{ks_comparison} object.}
}
\value{
A tibble.
}
\description{
These helpers reshape the long-format \code{value_diff} table inside a
\code{ks_comparison} into the four classic SAS PROC COMPARE output datasets:
}
\details{
\itemize{
\item \code{as_outbase()} — values from the base frame, only for matched rows that
contain at least one differing column; one row per matched key, one
column per compared variable.
\item \code{as_outcomp()} — same shape as \code{as_outbase()} but values from the
compare frame.
\item \code{as_outdif()} — numeric difference (\code{base - comp}) for every matched
row that contains at least one differing column; non-numeric cells are
reported as \code{NA} with a \code{.note} column.
\item \code{as_outnoequal()} — only the rows where at least one matched cell
differs, in long format (one row per differing cell).
}

The shapes mirror SAS's \verb{OUTBASE=}, \verb{OUTCOMP=}, \verb{OUTDIF=}, and
\verb{OUTNOEQUAL=} datasets in spirit; we do not replicate the exact metadata
columns (\verb{_TYPE_}, \verb{_OBS_}, ...) but include \code{key_id} and the original
key columns so the rows can be related back to the source frames.
}
\examples{
a <- data.frame(id = 1:3, x = c(1, 2, 3), y = c("a", "b", "c"))
b <- data.frame(id = 1:3, x = c(1, 2, 4), y = c("a", "B", "c"))
cmp <- ks_compare(a, b, by = "id")
as_outbase(cmp)
as_outcomp(cmp)
as_outdif(cmp)
as_outnoequal(cmp)
}
