% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ks_suggest_key}
\alias{ks_suggest_key}
\title{Suggest additional key columns when duplicates exist}
\usage{
ks_suggest_key(x, base = NULL, comp = NULL, top_n = 10L)
}
\arguments{
\item{x}{A \code{ks_comparison}.}

\item{base, comp}{Optional: the original input data frames used to
build \code{x}. Required to inspect candidate column uniqueness.}

\item{top_n}{Maximum number of candidate columns to return.}
}
\value{
A tibble with columns \code{column}, \code{pct_unique_base},
\code{pct_unique_comp}, \code{would_make_unique}. Zero rows when no key is
in use, no duplicates exist, or no improvement is possible.
}
\description{
Scans matched columns to find candidates whose combination with the
current \code{by} key would make the join key unique on at least one
side (and ideally both). Returns columns ranked by how much they
reduce duplicate-key cardinality. Useful when \code{\link[=ks_compare]{ks_compare()}} used
\code{dup_keys = "keep_all"} and the result looks suspicious.
}
\details{
Because \code{ks_compare()} does not snapshot the input data, the
original \code{base} and \code{comp} frames must be passed in to inspect
candidate columns. When \code{base} / \code{comp} are not supplied (or do
not contain the original columns) an empty tibble is returned.
}
\seealso{
\code{\link[=ks_match_health]{ks_match_health()}}, \code{\link[=ks_compare]{ks_compare()}}.
}
