% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{ks_options}
\alias{ks_options}
\title{Comparison options}
\usage{
ks_options(
  na_equal = TRUE,
  sas_special_missing = TRUE,
  compare_labels = TRUE,
  compare_formats = TRUE,
  str_trim = FALSE,
  str_case = c("sensitive", "fold"),
  str_norm = c("none", "NFC"),
  tz = c("preserve", "UTC", "strip")
)
}
\arguments{
\item{na_equal}{Treat two \code{NA}s as equal? Default \code{TRUE}. With
\code{FALSE}, any cell where one side is \code{NA} and the other is not is
reported as a value diff; cells where \emph{both} sides are \code{NA} are
also reported (mirroring SAS \verb{PROC COMPARE} behaviour with the
\code{NOMISS} option turned off).}

\item{sas_special_missing}{Distinguish SAS special missings (\code{.A}-\code{.Z}
and \code{._}) when comparing numeric columns imported via \code{haven}?
Default \code{TRUE}. When \code{TRUE}, \code{.A} and \code{.B} compare as different
even though both are \code{NA} in R; relies on the \code{tagged_na} tag
attached by \code{haven::read_sas()}.}

\item{compare_labels}{Compare \code{attr(x, "label")} between matched
columns and surface differences in \code{cmp$schema_diff}? Default
\code{TRUE}. Set to \code{FALSE} to suppress label-only diffs (e.g. when
metadata drift is expected).}

\item{compare_formats}{Compare \code{attr(x, "format.sas")} between
matched columns? Default \code{TRUE}. Comparison is trailing-dot- and
case-tolerant, so \code{DATE9.}, \code{DATE9}, and \code{date9.} all compare as
equal.}

\item{str_trim}{Trim leading/trailing whitespace with
\code{\link[stringi:stri_trim]{stringi::stri_trim_both()}} before comparing strings? Default
\code{FALSE}. Useful when one source has been padded by a fixed-width
exporter.}

\item{str_case}{One of \code{"sensitive"} (default) or \code{"fold"}. When
\code{"fold"}, strings are compared after Unicode case folding
(\code{stringi::stri_trans_tolower()}).}

\item{str_norm}{One of \code{"none"} (default) or \code{"NFC"}. When \code{"NFC"},
strings are Unicode-normalised before comparing so that
visually-identical sequences with different code-point
compositions match.}

\item{tz}{One of \code{"preserve"} (default), \code{"UTC"}, or \code{"strip"}.
Controls how \code{POSIXct} columns are reconciled when the two sides
carry different \code{tzone} attributes:
\itemize{
\item \code{"preserve"}: values are compared as-is (a tz mismatch surfaces
as a diff if it changes the underlying instant).
\item \code{"UTC"}: both sides are converted to UTC before compare.
\item \code{"strip"}: \code{tzone} is dropped and values compared as POSIXct.
}}
}
\value{
A \code{ks_options} S3 list.
}
\description{
Builds an options object consumed by \code{\link[=ks_compare]{ks_compare()}} that controls
missing-value semantics, label / format comparison, string
normalisation, and time-zone handling.
}
\examples{
# Defaults: strict NAs, label & format comparison on
ks_options()

# Loose strings: trim padding and ignore case
ks_options(str_trim = TRUE, str_case = "fold")

# Suppress label/format drift, but keep cell-level strictness
ks_options(compare_labels = FALSE, compare_formats = FALSE)

# Treat any NA as a difference (PROC COMPARE without NOMISS)
ks_options(na_equal = FALSE)
}
