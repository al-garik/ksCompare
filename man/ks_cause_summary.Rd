% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ks_cause_summary}
\alias{ks_cause_summary}
\title{Diff causes summary}
\usage{
ks_cause_summary(x)
}
\arguments{
\item{x}{A \code{ks_comparison}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{cause}: short label (e.g. \code{"letter case differs"}).
\item \code{n_cells}: number of cells contributing this cue.
\item \code{n_columns}: number of distinct base columns affected.
\item \code{columns}: comma-separated sample of the affected columns
(capped at 5 names).
Sorted by \code{n_cells} descending. Zero-row tibble when there are no
value differences.
}
}
\description{
Aggregates the \code{note} column of \code{cmp$value_diff} into a tidy
taxonomy: one row per distinct cause, with counts and the columns
it affects. Helps answer "what kinds of discrepancies do we have?"
before drilling into individual cells.
}
\details{
Compound notes (multiple cues joined by \code{"; "}) are split into
their elementary cues, so a single cell can contribute to several
causes. Cells with \code{note = NA} (a "plain" value change with no
recognised cue) are bucketed as \code{"plain value change"}.
}
\examples{
a <- data.frame(id = 1:3, x = c("a", "b ", "c"), y = c(1, 2, 3))
b <- data.frame(id = 1:3, x = c("A", "b",  "c"), y = c(1, 2, 4))
cmp <- ks_compare(a, b, by = "id")
ks_cause_summary(cmp)
}
\seealso{
\code{\link[=ks_compare]{ks_compare()}}, \code{\link[=ks_row_diff_summary]{ks_row_diff_summary()}}.
}
