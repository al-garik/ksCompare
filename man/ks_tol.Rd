% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{ks_tol}
\alias{ks_tol}
\title{Tolerance specification for numeric comparisons}
\usage{
ks_tol(abs = 0, rel = 0, ulp = 0, per_column = NULL)
}
\arguments{
\item{abs}{Non-negative absolute tolerance (default \code{0} — strict
equality).}

\item{rel}{Non-negative relative tolerance, scaled by
\code{max(abs(a), abs(b))} (default \code{0}).}

\item{ulp}{Non-negative integer ULP tolerance. Typical values are
\code{4}–\code{16}; defaults to \code{0}.}

\item{per_column}{Optional named list of per-column \code{\link[=ks_tol]{ks_tol()}}
overrides keyed by base-side column name.}
}
\value{
A \code{ks_tol} S3 list.
}
\description{
Builds a tolerance object consumed by \code{\link[=ks_compare]{ks_compare()}}. Two numeric
values \code{a} and \code{b} are considered equal when \strong{any} of the active
rules pass:
}
\details{
\itemize{
\item \code{abs(a - b) <= abs}
\item \code{abs(a - b) <= rel * max(abs(a), abs(b))}
\item \code{ulp_distance(a, b) <= ulp} — IEEE-754 units in the last place,
useful for catching floating-point round-trip noise without false
positives on genuine differences.
}

Per-column overrides may be supplied via \code{per_column}, a named list
whose names are \strong{base-side} column names and whose values are
themselves the result of \code{ks_tol()}. Columns not listed fall back to
the top-level \code{abs} / \code{rel} / \code{ulp}.
}
\examples{
ks_tol(abs = 1e-9)
ks_tol(rel = 1e-6, per_column = list(price = ks_tol(abs = 0.005)))
ks_tol(ulp = 4)
}
