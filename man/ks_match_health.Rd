% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ks_match_health}
\alias{ks_match_health}
\title{Match-quality health check}
\usage{
ks_match_health(x)
}
\arguments{
\item{x}{A \code{ks_comparison}.}
}
\value{
A list with components:
\itemize{
\item \code{diff_density}: \code{n_value_diffs / (n_matched_rows * n_matched_columns)} (0-1).
\item \code{n_fully_diff_rows}: matched rows where every matched column
differs (or, more permissively, >= 80\% of columns differ).
\item \code{pct_fully_diff_rows}: as proportion of matched rows.
\item \code{n_value_to_na}, \code{n_na_to_value}: structural NA transitions.
\item \code{dup_keys}: \code{TRUE} if either side had duplicate key values.
\item \code{dup_positional}: \code{TRUE} if duplicates were paired positionally
(\code{keep_all}) — the most error-prone path.
\item \code{row_count_delta}: \code{n_comp_rows - n_base_rows} (signed).
\item \code{flags}: character vector of human-readable warnings (possibly
empty).
\item \code{severity}: \code{"ok"}, \code{"info"}, \code{"warn"}, or \code{"critical"}.
}
}
\description{
Computes a small set of metrics that flag \emph{suspicious} matching:
high diff density, many fully-different rows, duplicate keys
resolved positionally, and similar patterns that often indicate
the wrong key was used.
}
\examples{
a <- data.frame(id = c(1, 1, 2), x = c(1, 2, 3))
b <- data.frame(id = c(1, 1, 2), x = c(9, 9, 3))
cmp <- ks_compare(a, b, by = "id", dup_keys = "keep_all")
ks_match_health(cmp)
}
\seealso{
\code{\link[=ks_recommendations]{ks_recommendations()}}, \code{\link[=ks_compare]{ks_compare()}}.
}
