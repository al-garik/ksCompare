% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unmatched.R
\name{ks_unmatched_rows}
\alias{ks_unmatched_rows}
\title{Unmatched rows from a comparison}
\usage{
ks_unmatched_rows(x, side = c("both", "base_only", "comp_only"))
}
\arguments{
\item{x}{A \code{ks_comparison}.}

\item{side}{One of \code{"both"} (default), \code{"base_only"}, or
\code{"comp_only"}.}
}
\value{
A tibble with columns \code{side}, \code{key_id}, \code{key_label},
\code{base_row}, \code{comp_row}, followed by the data columns from the
side that holds each row (\code{base_row} is \code{NA} for \code{comp_only}
rows and vice versa, making it trivial to look an observation
up in the original \code{base} / \code{comp} frame).
Empty (zero-row) tibble when there are no unmatched rows.
}
\description{
Returns the rows that are present on only one side of a comparison
(\code{"base_only"} or \code{"comp_only"}), with their original data columns.
Mirrors the "Observations in \if{html}{\out{<side>}} only" tables produced by SAS
\verb{PROC COMPARE}.
}
\details{
The result is precomputed at \code{\link[=ks_compare]{ks_compare()}} time and capped by the
\code{max_unmatched_rows} argument (default \code{100}). When the cap kicks
in, the \code{truncated} attribute is set and the full counts are
recorded on \code{n_total}.
}
\examples{
a <- data.frame(id = 1:3, x = c(1, 2, 3))
b <- data.frame(id = 2:4, x = c(2, 3, 4))
cmp <- ks_compare(a, b, by = "id")
ks_unmatched_rows(cmp)
}
\seealso{
\code{\link[=ks_compare]{ks_compare()}}, \code{\link[=ks_tidy]{ks_tidy()}}.
}
