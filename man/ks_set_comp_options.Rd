% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{ks_set_comp_options}
\alias{ks_set_comp_options}
\title{Set ksCompare global options}
\usage{
ks_set_comp_options(...)
}
\arguments{
\item{...}{Named arguments. Recognised names match the formals of
\code{\link[=ks_comp_options]{ks_comp_options()}}: \code{na_equal}, \code{sas_special_missing},
\code{compare_labels}, \code{compare_formats}, \code{str_trim}, \code{str_case},
\code{str_norm}, \code{tz}, \code{path}. A single unnamed \code{ks_comp_options}
object may also be passed in, in which case all of its fields
are pushed.}
}
\value{
Invisibly, a named list of the previous values (suitable for
\code{do.call(options, prev)} to restore).
}
\description{
Thin wrapper around \code{\link[base:options]{base::options()}} for the \verb{ksCompare.*} namespace.
Accepts the same argument names as \code{\link[=ks_comp_options]{ks_comp_options()}} (without the
\code{ksCompare.} prefix) and forwards them into \code{\link[base:options]{base::options()}},
returning the previous values invisibly so the call can be unwound
with \code{\link[base:options]{base::options()}} or \code{\link[withr:with_options]{withr::with_options()}}.
}
\details{
Globals set this way become the new defaults of
\code{\link[=ks_comp_options]{ks_comp_options()}} (and therefore of \code{\link[=ks_compare]{ks_compare()}} when no
\verb{options =} argument is supplied).
}
\examples{
old <- ks_set_comp_options(path = tempfile("ksCompare_"), str_case = "fold")
ks_comp_options()$path
do.call(options, old)  # restore
}
