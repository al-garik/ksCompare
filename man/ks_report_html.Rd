% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-html.R
\name{ks_report_html}
\alias{ks_report_html}
\title{Render a self-contained HTML report}
\usage{
ks_report_html(
  x,
  path = NULL,
  title = "ksCompare report",
  subtitle = NULL,
  max_rows = 100L,
  theme = c("default", "slate"),
  group_by_key = FALSE,
  max_groups = 200L
)
}
\arguments{
\item{x}{A \code{ks_comparison} object.}

\item{path}{File path for the report. Three behaviours:
\itemize{
\item missing / \code{NULL} (default): a filename is auto-generated from the
comparison's \code{base_name} (and, when distinct, \code{comp_name})
written into \code{options$path} (if set on
\code{\link[=ks_comp_options]{ks_comp_options()}}) or the current working directory, e.g.
\code{ksCompare_adsl_vs_adsl_qc.html}.
\item \code{NA}: nothing is written; the assembled \code{htmltools::tagList()} is
returned for embedding in another document.
\item a character path: written to that path. A \code{.html} extension is
appended if missing. A bare filename (no directory component) is
resolved relative to \code{options$path} when that has been set on
\code{\link[=ks_comp_options]{ks_comp_options()}}, otherwise relative to the working directory.
}}

\item{title}{Title shown at the top of the report.}

\item{subtitle}{Optional subtitle (e.g. study identifier or run id).}

\item{max_rows}{Per-table row cap (default \code{100}). When the value-diff
table exceeds this cap, the report shows a \emph{smart stratified sample}
covering each column and each distinct diff-cause (\code{note}), prioritising
the largest numeric magnitudes (at least one example per column and per
cause is always retained). A notice indicates the sample size and
recommends \code{as_tibble(cmp)} for the full set. \code{as_tibble(x)} is never
truncated.}

\item{theme}{One of \code{"default"} (steel blue) or \code{"slate"} (neutral
dark headers on light background).}

\item{group_by_key}{Logical (default \code{FALSE}). When \code{TRUE} and a \verb{by =}
key was used in \code{\link[=ks_compare]{ks_compare()}}, the value-diff section is rendered as
one collapsed \verb{<details>} block per key value, sorted by number of
diffs (most-affected first). For \code{dup_keys = "keep_all"} /
\code{"all_pairs"}, each block also shows a \emph{Pair} column so you can tell
pairs apart. Silently falls back to the flat table when no \verb{by =} was
supplied (position match).}

\item{max_groups}{Maximum number of key-value blocks to render when
\code{group_by_key = TRUE} (default \code{200}). Excess groups are summarised in
a closing notice.}
}
\value{
Invisibly returns \code{path} (or the assembled tag list when
\code{path = NULL}).
}
\description{
Produces a polished, single-file HTML report summarising a
\code{ks_comparison}. The output uses \code{htmltools} for layout and \code{reactable}
for interactive tables. No internet access, no Quarto, and no Pandoc
are required.
}
\details{
Layout: a left-hand sticky table of contents, a header with dataset
names, a status pill, KPI cards, then sections for schema, row diff,
value diff, patterns, and the run manifest. Tables use
friendly column labels, column groups, and \code{tick}/\code{cross} markers
instead of raw \code{TRUE}/\code{FALSE}.
}
\examples{
\dontrun{
  cmp <- ks_compare(iris, iris, by = "Species")

  # Explicit path
  ks_report_html(cmp, "report.html", title = "ADSL QC")

  # Auto-generated filename in working directory
  ks_report_html(cmp)

  # In-memory tagList for embedding
  tags <- ks_report_html(cmp, path = NA)
}
}
