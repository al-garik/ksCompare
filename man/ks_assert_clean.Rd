% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pointblank.R
\name{ks_assert_clean}
\alias{ks_assert_clean}
\alias{ks_pointblank_step}
\title{Use a comparison as a pipeline gate}
\usage{
ks_assert_clean(
  x,
  max_value_diffs = 0L,
  max_schema_diffs = 0L,
  max_unmatched_rows = 0L
)

ks_pointblank_step(
  agent,
  comparison,
  max_value_diffs = 0L,
  max_schema_diffs = 0L,
  max_unmatched_rows = 0L,
  label = "ksCompare clean"
)
}
\arguments{
\item{x}{A \code{ks_comparison} object returned by \code{\link[=ks_compare]{ks_compare()}}.}

\item{max_value_diffs}{Integer. Maximum number of differing cells
allowed in matched rows. Default \code{0L} (strict).}

\item{max_schema_diffs}{Integer. Maximum schema differences allowed,
counted as the sum of base-only columns, comp-only columns, and
matched columns whose \code{kind} differs. Default \code{0L} (strict).}

\item{max_unmatched_rows}{Integer. Maximum number of unmatched rows
allowed (\code{n_base_only_rows + n_comp_only_rows}). Default \code{0L}
(strict).}

\item{agent}{A \code{ptblank_agent} (typically piped in from
\code{\link[pointblank:create_agent]{pointblank::create_agent()}}) or a data frame / tibble. The
comparison gate is added as a step on this object and \code{agent} is
returned, so it composes with the rest of a pointblank pipeline.}

\item{comparison}{A \code{ks_comparison} produced by \code{\link[=ks_compare]{ks_compare()}} that
the step will gate on.}

\item{label}{Optional label for the pointblank step.}
}
\value{
\code{\link[=ks_assert_clean]{ks_assert_clean()}} returns \code{x} invisibly when expectations
are met. On failure, raises a condition of class
\code{ksCompare_assertion_failed} (catchable in CI with
\code{tryCatch(..., ksCompare_assertion_failed = function(e) ...)}).
\code{\link[=ks_pointblank_step]{ks_pointblank_step()}} returns a \code{pointblank} step.
}
\description{
Treats a \code{ks_comparison} as a pass/fail check. By default a
"passing" comparison has zero schema, key, and value differences;
callers can relax those expectations to allow a budget of known
acceptable diffs.
}
\details{
Two flavours are provided:
\itemize{
\item \code{\link[=ks_assert_clean]{ks_assert_clean()}} throws a classed error
(\code{ksCompare_assertion_failed}) and halts a pipeline when
expectations are unmet. Returns the comparison invisibly on
success, so it can be dropped into a \verb{|>} chain.
\item \code{\link[=ks_pointblank_step]{ks_pointblank_step()}} returns a \code{pointblank} step that wraps
\code{\link[=ks_assert_clean]{ks_assert_clean()}} for use inside \code{\link[pointblank:create_agent]{pointblank::create_agent()}} /
\code{\link[pointblank:action_levels]{pointblank::action_levels()}} flows.
}
}
\examples{
a <- data.frame(id = 1:3, x = 1:3)
ks_compare(a, a, by = "id") |> ks_assert_clean()

# Allow a small budget of known diffs
\dontrun{
  ks_compare(a, b, by = "id") |>
    ks_assert_clean(max_value_diffs = 5)
}

}
