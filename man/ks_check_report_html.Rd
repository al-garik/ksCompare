% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-report-html.R
\name{ks_check_report_html}
\alias{ks_check_report_html}
\title{Render an HTML check report}
\usage{
ks_check_report_html(
  data,
  path = NULL,
  title = "ksCompare check report",
  subtitle = NULL,
  col = check_msgs,
  max_rows = 500L,
  theme = c("default", "slate")
)
}
\arguments{
\item{data}{A data frame containing a check-message column.}

\item{path}{Output file path. Three behaviours:
\itemize{
\item \code{NULL} (default): auto-generates a file name in the current
working directory.
\item \code{NA}: returns the assembled \code{htmltools::tagList()} without
writing a file.
\item character path: writes report to that path (\code{.html} appended
if missing).
}}

\item{title}{Report title.}

\item{subtitle}{Optional subtitle.}

\item{col}{\code{<tidy-select>} Bare name of the check-message column.
Default \code{check_msgs}.}

\item{max_rows}{Maximum number of rows shown in the detail table.}

\item{theme}{One of \code{"default"} or \code{"slate"}.}
}
\value{
Invisibly returns \code{path} (or the assembled tag list when
\code{path = NA}).
}
\description{
Creates a self-contained HTML report for validation results produced by
\code{\link{ks_check_rules}}. The report includes overview KPI cards,
a violation summary table, and a row-level detail table.
}
\examples{
\dontrun{
checked <- ks_check_rules(adsl, rules, mode = "all")

# Write to disk
ks_check_report_html(checked, "check-report.html")

# In-memory tagList
tags <- ks_check_report_html(checked, path = NA)
}
}
