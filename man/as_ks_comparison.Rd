% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop-arsenal.R
\name{as_ks_comparison}
\alias{as_ks_comparison}
\title{Convert an \code{arsenal::comparedf} result into a \code{ks_comparison}}
\usage{
as_ks_comparison(x, ...)
}
\arguments{
\item{x}{An \code{arsenal::comparedf} object.}

\item{...}{Additional arguments forwarded to \code{\link[=ks_compare]{ks_compare()}} (e.g.
\code{tolerance}, \code{options}).}
}
\value{
A \code{ks_comparison} object.
}
\description{
Lossy interop: takes the two source frames stored on a
\code{arsenal::comparedf} object and re-runs \code{\link[=ks_compare]{ks_compare()}} using the same
BY keys. Useful for teams migrating from \code{arsenal::comparedf()} to
ksCompare without rewriting their inputs.
}
\details{
Note that ksCompare's diff is computed from scratch — it does not
attempt to translate \code{comparedf}'s internal diff tables row-by-row.
}
