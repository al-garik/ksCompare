% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{ks_compare}
\alias{ks_compare}
\title{Compare two data frames}
\usage{
ks_compare(
  base,
  comp,
  by = NULL,
  mapping = NULL,
  tolerance = ks_tol(),
  coerce = c("safe", "strict", "lossy"),
  dup_keys = c("first", "last", "keep_all", "all_pairs", "error"),
  allow_fuzzy_columns = FALSE,
  options = ks_comp_options(),
  base_name = NULL,
  comp_name = NULL
)
}
\arguments{
\item{base, comp}{Either an in-memory data frame (or anything coercible
via \code{\link[tibble:as_tibble]{tibble::as_tibble()}}: tibbles, data.tables, Arrow tables) or a
single character file path. When given a path, the file is read
automatically based on its extension:
\itemize{
\item \code{.sas7bdat}, \code{.xpt} (via \code{haven})
\item \code{.parquet}, \code{.feather} / \code{.arrow} (via \code{arrow})
\item \code{.rds}, \code{.rdata} / \code{.rda}
\item \code{.csv}, \code{.tsv} / \code{.txt}
}

\code{base} is treated as the reference; \code{comp} is the candidate. When
both inputs are file paths that share the same basename (e.g.
\code{prod/adsl.sas7bdat} vs \code{qc/adsl.sas7bdat}), display names are
automatically disambiguated with the parent directory.}

\item{by}{Optional column name(s) to use as the matching key.
\itemize{
\item \code{NULL} (default): rows are matched by row position. A warning
banner is shown in the HTML report so this is never silent.
\item \code{character}: same column name on both sides. Use a named vector
when the key is renamed: \code{c(USUBJID = "SUBJID")}.
\item \code{"auto"}: search the smallest combination of shared columns that
is unique on \strong{both} sides (capped at 4 columns). Falls back to
position match with a warning if nothing qualifies.
}}

\item{mapping}{Optional explicit column mapping for renamed columns.
Either a fully named character vector
(\code{c(base_col = "comp_col", ...)}) or a 2-column data frame with
columns \code{base} and \code{comp}. Mapping pairs always win over
exact-name matching.}

\item{tolerance}{A \code{\link[=ks_tol]{ks_tol()}} specification. Defaults to strict
equality. Per-column overrides supported via
\code{ks_tol(per_column = list(col = ks_tol(...)))}.}

\item{coerce}{Type-coercion strictness. One of:
\itemize{
\item \code{"safe"} (default): \code{vctrs::vec_ptype2()} plus integer↔double,
factor↔character, \code{haven_labelled} unwrapping.
\item \code{"strict"}: only common types \code{vctrs::vec_ptype2()} agrees on.
Type mismatches surface as \code{kind = "type_mismatch"} rows.
\item \code{"lossy"}: additionally allows numeric↔character,
date/datetime↔character (ISO-8601), factor↔integer (level codes).
}}

\item{dup_keys}{Strategy for duplicate keys (per-side independently).
One of:
\itemize{
\item \code{"first"} (default): keep the first occurrence per side. Drops
rows; emits a \code{ksCompare_dup_keys_resolved} info message.
\item \code{"last"}: keep the last occurrence per side. Same message.
\item \code{"keep_all"}: pair duplicate rows positionally within each key
group (1↔1, 2↔2, …). Leftover rows on the longer side become
base- or compare-only.
\item \code{"all_pairs"}: cartesian-pair every base row with every comp row
sharing a key value. Emits a
\code{ksCompare_all_pairs_cardinality} warning when group sizes
disagree.
\item \code{"error"}: raise on any duplicate key.
}}

\item{allow_fuzzy_columns}{Reserved for future use; must be \code{FALSE}.
Fuzzy column rename \emph{suggestions} are always returned on
\code{cmp$column_suggestions} (when \code{stringdist} is installed) but are
never applied silently.}

\item{options}{A \code{\link[=ks_comp_options]{ks_comp_options()}} specification controlling NA /
SAS special-missing semantics, label / format comparison, string
normalisation, and the default output folder for reports.}

\item{base_name, comp_name}{Optional display names for the two frames
(used in \code{print()} and reports). Default to the names of the
supplied expressions.}
}
\value{
A \code{ks_comparison} object with components:
\itemize{
\item \code{meta}: counts, keys, matching strategy, row-key lookup table.
\item \code{schema_diff}: one row per matched / unmatched column with kind
/ label / format comparison.
\item \code{column_suggestions}: fuzzy rename candidates (or empty).
\item \code{key_diff}: counts of matched / base-only / comp-only rows.
\item \code{row_diff}: long table of \code{key_id}, \code{base_row}, \code{comp_row},
\code{status}.
\item \code{value_diff}: long table of differing cells with \code{column_base},
\code{column_comp}, \code{kind}, \code{base}, \code{comp}, \code{diff}, \code{note}.
\item \code{pattern_summary}: detected recurring shapes per column.
\item \code{options}, \code{tolerance}: the inputs (echoed for the manifest).
\item \code{manifest}: input hashes + run metadata.
}

Inspect with \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[tibble:as_tibble]{tibble::as_tibble()}}, or
render via \code{\link[=ks_report_html]{ks_report_html()}} / \code{\link[=ks_report_xlsx]{ks_report_xlsx()}}.
}
\description{
\code{ks_compare()} is the single entry point for the package. It compares
\code{base} against \code{comp} and returns a \code{ks_comparison} S3 object containing
schema, key, row, and value differences plus a small QC manifest.
}
\section{Pipeline}{
\enumerate{
\item \strong{Schema alignment} — columns are paired by an explicit \code{mapping}
first, then by exact name match. Unmatched columns become
\code{base_only} / \code{comp_only} and are reported in \code{cmp$schema_diff}.
\item \strong{Key resolution} — \code{by} is taken literally if supplied,
auto-inferred from shared columns when \code{by = "auto"}, or rows are
matched by row position when \code{by = NULL}.
\item \strong{Row matching} — keyed-unique join, duplicate-key resolution
(\code{dup_keys}), or position-zip; details land in
\code{cmp$meta$matching} (also displayed by \code{\link[=print]{print()}} and in the HTML
report).
\item \strong{Cell diff} — per-column equality respecting \code{\link[=ks_tol]{ks_tol()}} (abs /
rel / ULP) for numerics, \code{\link[=ks_comp_options]{ks_comp_options()}} for strings / NAs / SAS
special missings, and \code{vctrs::vec_cast()} for compatible types.
\item \strong{Pattern detection} — recurring shapes across diffs (constant
offset, sign flip, trim-only, …) are scored and surfaced on
\code{cmp$pattern_summary}.
\item \strong{Manifest} — \code{xxhash64} digests of inputs / options / tolerance
plus a timestamp and package version, for QC traceability.
}
}

\examples{
a <- data.frame(id = 1:3, x = c(1, 2, 3), y = c("a", "b", "c"))
b <- data.frame(id = 1:3, x = c(1, 2, 4), y = c("a", "B", "c"))

# Strict compare on `id`
cmp <- ks_compare(a, b, by = "id")
cmp
tibble::as_tibble(cmp)

# Tolerant numeric compare
ks_compare(
  data.frame(id = 1, x = 1.0),
  data.frame(id = 1, x = 1.0001),
  by = "id",
  tolerance = ks_tol(abs = 1e-3)
)

# Renamed key column
ks_compare(
  data.frame(USUBJID = "S001", x = 1),
  data.frame(SUBJID  = "S001", x = 1),
  by = c(USUBJID = "SUBJID")
)
}
