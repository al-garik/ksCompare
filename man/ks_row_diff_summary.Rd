% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ks_row_diff_summary}
\alias{ks_row_diff_summary}
\title{Per-row diff summary}
\usage{
ks_row_diff_summary(x, n = NULL)
}
\arguments{
\item{x}{A \code{ks_comparison}.}

\item{n}{Optional cap on the number of rows returned (default
\code{NULL} = all). When set, rows beyond \code{n} are dropped.}
}
\value{
A tibble with one row per matched observation that has at
least one cell diff:
\itemize{
\item \code{key_id}, \code{base_row}, \code{comp_row}, \code{key_label}
\item \code{n_diffs}: count of differing cells on this observation.
\item \code{columns}: comma-separated sample of the affected columns
(capped at 8 names).
Sorted by \code{n_diffs} descending. Zero-row tibble when there are no
value differences.
}
}
\description{
Tallies the number of cell differences per matched observation,
sorted descending. Useful for spotting rows where most/all columns
disagree (often a sign of a wrong row match, a duplicated key
resolved differently, or a systemic shift on a single record).
}
\details{
Only matched rows are reported; unmatched rows are available via
\code{\link[=ks_unmatched_rows]{ks_unmatched_rows()}}.
}
\examples{
a <- data.frame(id = 1:3, x = 1:3, y = 1:3)
b <- data.frame(id = 1:3, x = c(1, 9, 9), y = c(1, 9, 3))
cmp <- ks_compare(a, b, by = "id")
ks_row_diff_summary(cmp)
}
\seealso{
\code{\link[=ks_compare]{ks_compare()}}, \code{\link[=ks_cause_summary]{ks_cause_summary()}}.
}
