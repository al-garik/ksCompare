# ks_align_columns rejects unknown mapping columns

    Code
      ks_align_columns(a, b, mapping = c(z = "y"))
    Condition
      Error in `ks_align_columns()`:
      ! Column "z" in `mapping` not found in base.

